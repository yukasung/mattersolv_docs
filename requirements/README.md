# Requirements Source Files

Use this folder for source requirement files that Codex should read before
writing or updating the MatterSolv Documentation site.

## Folder Structure

- `source/` - Original requirement sources (`.docx`, `.xlsx`, `.pdf`, images)
- `extracted/` - Generated Markdown summaries from source files, when needed

Published documentation pages live under `docs/app/docs/`.
